<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.1//EN"
    "http://struts.apache.org/dtds/struts-2.1.dtd">

<struts>
    <package name="cart" namespace="/cart" extends="struts-default,json-default">

        <!-- ✅ AJAX 加入購物車：回傳 JSON 給 JS -->
        <action name="add" class="cartAction" method="addToCart">
            <result name="json" type="json">
                <param name="root">jsonResult</param>
            </result>
        </action>

        <!-- ✅ 顯示購物車內容頁：使用 Spring Bean -->
        <action name="view" class="cartViewAction" method="execute">
            <result name="success">/WEB-INF/views/cart/cart.jsp</result>
        </action>
        <!-- 清空指定使用者的購物車 -->
		<action name="clear" class="com.example.action.CartClearAction">
		    <result name="success" type="redirect">/cart/view.action</result>
		</action>

		
    </package>
</struts>
<!-- <!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.1//EN"
    "http://struts.apache.org/dtds/struts-2.1.dtd">

<struts>
    <package name="cart" namespace="/cart" extends="struts-default,json-default">

        ✅ AJAX 加入購物車：回傳 JSON 給 JS
        <action name="add" class="com.example.action.CartAction" method="addToCart">
            <result name="json" type="json">
                <param name="root">jsonResult</param>
            </result>
        </action>

        ✅ 顯示購物車內容頁
        <action name="view" class="com.example.action.CartViewAction">
            <result name="success">/WEB-INF/views/cart/cart.jsp</result>
        </action>

    </package>
</struts>
 -->